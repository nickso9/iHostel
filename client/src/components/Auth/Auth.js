import React from 'react';



const Auth = () => {

    return (
        <nav className={'auth-btn'}>
            <button>Register</button>
            <button>Login</button>
        </nav>
    )

};

export default Auth